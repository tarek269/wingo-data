name: Wingo 1Min Signal (Telegram)

on:
  schedule:
    - cron: '*/1 * * * *'
  workflow_dispatch:

jobs:
  collect:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      
      - name: Download 1Min Data
        run: |
          curl -L -o data1m.json "https://draw.ar-lottery01.com/WinGo/WinGo_1M/GetHistoryIssuePage.json"
      
      - name: Process & Send Signal
        run: |
          python3 << 'EOF'
          import json, os, requests
          from datetime import datetime
          
          # ===== আপনার Telegram Credentials দিন =====
          BOT_TOKEN = "8601346832:AAENbaL-vErIXqayABEJMFqdxEQWxwt4stg"  # ← এখানে আপনার টোকেন দিন
          CHAT_ID = "6678981102"      # ← এখানে আপনার চ্যাট আইডি দিন
          # =========================================
          
          # ডেটা লোড
          with open('data1m.json') as f:
              data = json.load(f)
          
          # ইতিহাস লোড
          history = []
          if os.path.exists('history.json'):
              with open('history.json') as f:
